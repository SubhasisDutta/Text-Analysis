This module will be a MEAN Application that will make REST calls to different endpoints and get the query results.

npm init --> create the package.json
bower init --> create bower.json
bower install angular-material --save

Install/update bower globally
npm install -g bower

install Typescript
npm install typescript -g
tsc --version

Install Typescript devination
npm install -g tsd

Install angular and angular-material tsd's
tsd install angular angular-material --save

Live reload enable
npm install live-server -g

To keep running live script
do live-script from app root folder

To keep changing type script file to js files
tsc.cod -w on folder root of typescript files